@model List<CollegeManagementSystem.Models.Departments>
@{
    ViewBag.Title = "List of Departments";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";

}

@section css {}
<style>
    input#btnSearch {
        height: 31px;
        line-height: 30px;
        padding: 0px 11px;
        font-size: 13px;
        border-radius: 0 25px 25px 0;
    }

    .panel .panel-heading input[type=text] {
        border-radius: 25px 0 0 25px;
        box-shadow: none;
        border-color: none;
        height: 30px;
        border: none;
        margin-right: 0;
    }

    .btn-group-sm > .btn, .btn-sm i.fa.fa-plus-square {
        margin-right: 3px;
    }

    a.btn.btn-primary.btn-sm {
        border-radius: 25px;
    }

    .panel-heading .input-group {
        box-shadow: none;
    }
</style>
<div class="row container">
    <div class="col-md-12">
        <div class="panel panel-primary">
            <div class="panel-heading">
                <h4 class="panel-title pull-left">@ViewBag.Title</h4>
                <div class="input-group pull-right">
                  
                    <a class="btn btn-primary btn-sm" href="@Url.Action("CreateDepartment", "Master")"><i class="fa fa-plus-square"></i> ADD DEPARTMENT</a>
                </div>
                <div class="clearfix"></div>
            </div>
            <div class="panel-body">
                <div class="row">
                    <div class="col-lg-12">
                        <div class="table-responsive">
                            <table id="table" class="table">
                                <thead class="text-primary">
                                    <tr>
                                        <th>S.NO.</th>
                                        <th>DEPARTMENT</th>
                                        <th>DEPT CODE</th>
                                        <th>DEPT SHORT NAME</th>
                                        <th>ACTION(s)</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model.Select((data, index) => new { data, index }))
                                    {
                                        <tr>
                                            <td>
                                                @(item.index + 1)
                                            </td>
                                            <td>
                                                @Html.DisplayFor(modelItem => item.data.DeptName)
                                            </td>
                                            <td>
                                                @Html.DisplayFor(modelItem => item.data.DeptCode)
                                            </td>

                                            <td>
                                                @Html.DisplayFor(modelItem => item.data.DeptShortName)
                                            </td>
                                            
                                            <td>
                                                @Html.ActionLink("Edit", "EditDepartment", new { id = item.data.Id }) |
                                                @Html.ActionLink("Details", "DepartmentDetails", new { id = item.data.Id }) |
                                                @Html.ActionLink("Delete", "DeleteDepartment", new { id = item.data.Id }, new { onclick = "return confirm('Do you really want to delete this User?');" })
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section scripts {

}
